export default function SiteFooter() {
  return (
    <footer className="border-t border-black/10 bg-white/80">
      <div className="container flex flex-col gap-3 py-8 text-sm text-black/70">
        <p>MetaEnergy (元象) MVP. Membership, referrals, commissions, points, and simple frequency tools.</p>
        <p>Deployment target: Next.js App Router + Supabase + Vercel.</p>
      </div>
    </footer>
  );
}
